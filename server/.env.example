# Copy to server/.env and fill in. Keys live ONLY here — never in client code,
# never in logs, never echoed in API error responses, never exposed to web/.
# Extraction + rules Q&A run on OpenAI (see src/extraction/provider.ts).
OPENAI_API_KEY=sk-...
# Only needed if provider.ts is switched back to the Anthropic provider:
# ANTHROPIC_API_KEY=sk-ant-...
PORT=3001
